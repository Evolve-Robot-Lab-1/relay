{
  "name": "agent-network",
  "version": "1.0.0",
  "description": "Viral browser-based agent network - anonymous, instant, free",
  "main": "server.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node tests/client-syntax.mjs && wrangler deploy --dry-run",
    "test:browser": "node tests/browser-e2e.mjs",
    "test:e2e": "node tests/e2e.mjs",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
